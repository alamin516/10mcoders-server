{
    "version": 2,
    "builds": [
      {
        "src": "server.ts",
        "use": "@vercel/node",
        "config": {
          "includeFiles": ["dist/server.js"]
        }
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/dist/server.js"
      }
    ]
  }
  